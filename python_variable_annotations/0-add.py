#!/usr/bin/env python3
""" A module containing a function that handles float addition"""


def add(a: float, b: float) -> float:
    """
    function that adds two float

    Attributes:
        a(float): a float
        b(float): a float

    Returns:
        the result of a + b
    """
    return (float(a + b))
